<div>
    <div class="d-flex gap-3 align-items-center">
        <a href="#"
            class="rounded p-1 px-2 {{ $active == 'sell' ? 'bg-success text-white' : 'bg-light' }}  border text-dark">Sell
            Device</a>
        <a href="#"
            class="rounded p-1 px-2 {{ $active == 'buy' ? 'bg-success text-white' : 'bg-light' }}  border text-dark">Buy
            Device</a>
        <a href="#"
            class="rounded p-1 px-2 {{ $active == 'repair' ? 'bg-success text-white' : 'bg-light' }}  border text-dark">Repair
            Device</a>
    </div>
</div>
